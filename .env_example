# Ports für die lokale Erreichbarkeit (Host-Seite)
# Ändern, falls die Standard-Ports bereits belegt sind.
BACKEND_PORT=8087
FRONTEND_PORT=3000

# Sub-Pfad wird automatisch aus der Browser-URL erkannt – keine Konfiguration nötig.
