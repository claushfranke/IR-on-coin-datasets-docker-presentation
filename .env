# Ports für die lokale Erreichbarkeit (Host-Seite)
# Ändern, falls die Standard-Ports bereits belegt sind.
BACKEND_PORT=8087
FRONTEND_PORT=3000

# Optionaler Sub-Pfad für Reverse-Proxy-Setups (ohne abschließenden Slash)
# Beispiele: "" (Root), "/imagnum-piai"
APP_BASE_PATH=/imagnum-piai
